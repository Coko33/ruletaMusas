let numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9];

let numContainer = document.querySelector("#numero");

/* numeros.forEach((e) => {
  setInterval(() => {
    numContainer.innerHTML = e;
  }, 2000);
}); */
